import styled from 'styled-components';
import { Overlay } from '../../global-styles';

export const LoadingPageOverlay = styled(Overlay)`
  background-color: rgba(255, 255, 255, 0.5);
  opacity: 0.5;
`;
