import styled from 'styled-components';
import { Overlay } from '../../global-styles';

export const SubmitingFormOverlay = styled(Overlay)`
  background-color: rgba(255, 255, 255, 0.6);
  opacity: 0.6;
  position: absolute;
`;
